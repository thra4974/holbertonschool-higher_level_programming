#!/usr/bin/python3


def search_replace(my_list, search, replace):
    new_list = []
    for n in my_list:
        new_list.append(list((map(lambda x: x.replace(str(search), str(replace)), str(n)))))
    return (new_list)
