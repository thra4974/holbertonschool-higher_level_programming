#!/usr/bin/python3


def element_at(my_list, idx):
    for n in range(1, len(my_list)):
        if my_list.index(n) == idx:
            return (my_list.index(n))
        elif my_list.index(n) not in range(0, len(my_list)):
            return (None)
        else:
            return (None)
